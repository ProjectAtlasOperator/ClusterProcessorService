apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster_processor_service.fullname" . }}-test-connection"
  labels:
    {{- include "cluster_processor_service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster_processor_service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
